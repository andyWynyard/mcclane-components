import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Info" />

# Info

To use these components, you will need to install from npm.

`yarn add mcclane-components` or  
`npm i mcclane-components`
