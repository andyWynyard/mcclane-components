import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { Button } from '@storybook/react/demo'
import Header from '../Header'
import Typography from '../Typography'
import { defaultTheme } from '../../defaultTheme.ts'

<Meta
  title="Header"
  component={Header}
  argTypes={{
    clipPath: {
      name: 'clipPath',
      control: 'text',
      defaultValue: 'polygon(0 0, 100% 0, 100% 75vh, 0 100%)',
    },
    backgroundImage: {
      name: 'backgroundImage',
      control: 'text',
      defaultValue: 'https://picsum.photos/id/870/1000/800',
    },
    height: {
      name: 'height',
      control: 'text',
      defaultValue: '85vh',
    },
    children: {
      defaultValue: 'Hi there',
      control: {
        type: 'text',
      },
      table: {
        type: {
          summary: 'string',
        },
      },
    },
  }}
/>

## Header

<Preview>
  <Story name="Header">
    {(args) => {
      console.log(args)
      return (
        <Header {...args}>
          <Typography.H1 color="white">{args.children}</Typography.H1>
          <Typography.H5 color="white">{args.children}</Typography.H5>
        </Header>
      )
    }}
  </Story>
</Preview>

### Props

<Props story="Header" />
